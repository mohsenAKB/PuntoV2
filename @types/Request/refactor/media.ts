export interface ISetProjectMediaRequest {
  category_ids: number[],
  color_ids: number[],
  material_ids: number[],
  small_residential_space_ids: number[],
}

export interface ISetProjectCoverRequest {
  cover_attachment_id: number
}